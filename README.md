# MongoDB $inc Operator Error

This repository demonstrates a common error when using the `$inc` operator in MongoDB to increment a field's value.  The `$inc` operator expects a numerical value; attempting to increment a string will result in an error or unexpected results. The solution shows how to properly use `$inc`.